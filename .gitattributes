* text=auto

# YAML files must use LF (GitHub Actions parser requires it)
*.yml   text eol=lf
*.yaml  text eol=lf

# PowerShell scripts
*.ps1   text eol=crlf
*.bat   text eol=crlf

# Python files
*.py    text eol=lf
*.pyw   text eol=lf
